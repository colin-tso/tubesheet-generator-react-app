import { DiagramGrid, DiagramCard } from "../DiagramCard";
import { LayoutPatternDiagram } from "../LayoutPatternDiagram";

The five layout options are all built from the same three constants — the **row spacing** ($d_x$), the **row-to-row spacing** ($d_y$), and the **row stagger** ($C$) — computed once from the pitch.

- $d_x$: the spacing between neighbouring tubes *within the same row*.
- $d_y$: the vertical spacing between one row and the next.
- $C$: the horizontal offset applied to every other row. This is what staggers the pattern instead of stacking it in a plain grid.

Every other row shifts right by $C$; the rows in between stay aligned with the first. That two-row motif, repeated outward and then mirrored, is the entire pattern (see [Filling the circle](#/docs/filling)).

A tube’s nearest neighbour usually sits in the *adjacent* row rather than its own, so the pitch you asked for is recovered from the diagonal distance to that neighbour, not from $d_x$ directly:

:::Formula{id="pattern-pitch" label="Pitch, derived from dx, dy, C"}
$$
\begin{align}
P = \sqrt{C^2 + d_y^2}
\end{align}
$$
:::

The layout’s name (30°, 45°, 60°, or 90°) is the angle ($\theta$) between that diagonal pitch line and the vertical, row-to-row direction:

:::Formula{id="layout-angle" label="Layout angle"}
$$
\begin{align}
\theta = \arctan\!\left( \frac{C}{d_y} \right)
\end{align}
$$
:::

The table below gives each layout’s $d_x$, $d_y$, and $C$ in terms of the pitch, followed by a diagram of the same numbers. Each diagram draws the actual nearest-neighbour tubes: an apex tube plus two base tubes for the 30°/60° triangular patterns, or four tubes forming a square or diamond for the 45°/90° square patterns. The highlighted lines are true pitch-length sides, and the arc marks the angle.

| Layout | $d_x$ | $d_y$ | $C$ | Row spacing vs. pitch |
| --- | --- | --- | --- | --- |
| 30° | $P$ | $P \sin 60°$ | $P / 2$ | equal ($d_x = P$) |
| 60° | $2P \sin 60°$ | $P / 2$ | $d_x / 2$ | $d_x$ greater than $P$ |
| 90° | $P$ | $P$ | $0$ (square grid) | equal ($d_x = P$) |
| 45° | $P / \cos 45°$ | $d_x / 2$ | $d_x / 2$ | $d_x$ greater than $P$ |

<DiagramGrid>
  <DiagramCard title="30° Triangular">
    <LayoutPatternDiagram layout={30} />
  </DiagramCard>
  <DiagramCard title="60° Rotated Triangular" >
    <LayoutPatternDiagram layout={60} />
  </DiagramCard>
  <DiagramCard title="90° Square" >
    <LayoutPatternDiagram layout={90} />
  </DiagramCard>
  <DiagramCard title="45° Rotated Square" >
    <LayoutPatternDiagram layout={45} />
  </DiagramCard>
</DiagramGrid>

30° and 60° are the two equilateral-triangle (“triangular pitch”) patterns, rotated relative to each other. Each tube sits exactly one pitch from the nearest vertically or horizontally aligned tube.

45° and 90° are the same square (“diamond”) pattern in two orientations. Every side of that square is one pitch long, so its corner angle is always 90°, regardless of rotation.
